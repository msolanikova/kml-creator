export class TrailFileMeta {
  path: string;
  color: string;
  layer: string;

  constructor(path: string, color: string, layer: string) {
    this.path = path;
    this.color = color;
    this.layer = layer;
  }
}
